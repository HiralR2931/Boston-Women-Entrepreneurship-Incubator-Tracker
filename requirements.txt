streamlit>=1.35
pandas>=2.0
numpy>=1.24
plotly>=5.20
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
reportlab>=4.0

# Optional - only needed if you point the app at real MySQL / MongoDB
# instead of the bundled SQLite / JSON fallback (see database/db_config.py
# and nosql/nosql_store.py):
sqlalchemy>=2.0
pymysql>=1.1
pymongo>=4.6
